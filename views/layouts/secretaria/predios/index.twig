
<!-- Page Heading -->
	<div class="row">
    	<div class="col-lg-12">
        	<ol class="breadcrumb">
            	<li class="active">Predios</li>
        	</ol>
    	</div>
	</div>

	<div class="col-md-12">
		<button id="createPredio" class="btn btn-primary"><i class="fa fa-plus-square-o" aria-hidden="true"></i> Nuevo Predio</button><br><br>
		<table id="table" class="table table-condensed table-hover table-bordered table-responsive">
		 	<thead>
		 		<th>FICHA CATASTRAL</th>
		 		<th>MATRICULA INMOVILIARIA</th>
		 		<th>DIRECCION</th>
		 		<th>BARRIO</th>
		 		<th>VALOR DECLARADO</th>
				<th><i class="fa fa-pencil-square-o text-success"></i><i class="fa fa-plus text-warning" aria-hidden="true"></i></th>
		 	</thead>
		 	<tbody>
		 		{% for predio in predios  %}		 	
		 			<tr>
		 				<td>{{predio.num_ficha_catastral}}</td>
		 				<td>{{predio.num_matricula_inmoviliaria}}</td>
		 				<td>{{predio.direccion_predio}}</td>
		 				<td>{{predio.barrio_predio}}</td>
		 				<td>{{predio.valor_declarado}}</td>
		 				<td>
		 				<button id="{{predio.id_predial}}" class="btn btn-link editPredio"><i class="fa fa-pencil-square-o text-success"></i></button>
		 				<button id="{{predio.id_predial}}" class="btn btn-link agregarPropietario" data-toggle="modal" data-target="#modal-participantes-predio"><i class="fa fa-plus text-warning"></i></button>
		 				</td>
					 </tr>										
		 		{% endfor %}
		 	</tbody>
		 </table>
	</div>

	 <!-- Modal -->
	<div class="modal fade" id="modal-participantes-predio" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
	  <div class="modal-dialog modal-lg" role="document">
	    <div class="modal-content">
	      <div class="modal-header">
	        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
	        <h4 class="modal-title" id="myModalLabel">Dueños del Predio</h4>
	      </div>
	      <div class="modal-body-1">
	        <!--  formulario de participantes -->
	      </div>
	      <div class="modal-footer">
	        <button type="button" class="btn btn-danger" data-dismiss="modal">Cerrar</button>
	      </div>
	    </div>
	  </div>
	</div>



	 <!-- Page-Level Demo Scripts - Tables - Use for reference -->
    <script>
    $(document).ready(function() {
        tablas();
    });
    $('#createPredio').click(function(){  cargarCreatePredio(); });
    $('.editPredio').click(function(){ var id = $(this).attr('id'); cargarEditPredio(id); });
    $('.agregarPropietario').click(function(){ var id = $(this).attr('id'); agregarPropietarioPredio(id); });

    function tablas()
    {
    	$('#table').DataTable({
            responsive: true,
            "order": [[0, "desc"]],
					"language":{
					"lengthMenu": "Mostrar _MENU_ registros por pagina",
					"info": "Mostrando pagina _PAGE_ de _PAGES_",
						"infoEmpty": "No hay registros disponibles",
						"infoFiltered": "(filtrada de _MAX_ registros)",
						"loadingRecords": "Cargando...",
						"processing":     "Procesando...",
						"search": "Buscar:",
						"zeroRecords":    "No se encontraron registros coincidentes",
						"paginate": {
							"next":       "Siguiente",
							"previous":   "Anterior"
						},					
					},
        });
    }
    </script>